/**
 * Main Page — TraderUTC Earth
 *
 * Server component that renders the Dashboard (client component).
 * The Dashboard handles all client-side logic including the 3D canvas.
 */
import { Dashboard } from '@/features/dashboard';

export default function HomePage() {
  return <Dashboard />;
}
